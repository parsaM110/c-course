// HEADER Gaurd

#ifndef LIAREA_H // all capital because it have to be unique
// there is no namespace in c, in bigger programs also add version to name
// only do it header is enough
#define LIBAREA_H
double area(double r);

#endif